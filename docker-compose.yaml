version: '3.8'

services:
  server:
    build:
      context: ./contestr
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      mongodb:
        condition: service_healthy
        required: true
    environment:
      APP_MONGODB_URI: "mongodb://mongodb:27017"
      APP_MONGODB_DATABASE: "${MONGO_DATABASE}"
    env_file:
      - ./contestr/.env

  mongodb:
    image: mongo:latest
    restart: unless-stopped
    ports:
      - "15432:27017"
    healthcheck:
      test: [ "CMD", "mongosh", "--eval", "'db.runCommand({ping:1})'" ]
      interval: 5s
      timeout: 5s
      retries: 3
      start_period: 5s
    environment:
      MONGO_INITDB_DATABASE: "${MONGO_DATABASE}"
    volumes:
      - mongodb_data:/data/db
      - ./mongodb/mongo-init-scripts:/docker-entrypoint-initdb.d

volumes:
  mongodb_data:
